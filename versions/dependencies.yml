org:
  assertj:
    assertj-core: assertj-core

  apache:
    kafka:
      kafka_${scala.version}: kafka
    spark:
      spark-streaming-kafka_${scala.version}: spark-streaming-kafka

  codehaus:
    groovy:
      groovy: groovy
      groovy-all: groovy
      groovy-json: groovy

  projectlombok:
    lombok: lombok

  scala-lang:
    scala-library: scala-full

com:
  asprise:
    ocr:
      java-ocr-api: java-ocr-api
  datastax:
    spark:
      spark-cassandra-connector_${scala.version}: spark-cassandra-connector

  google:
    guava:
      guava: guava
junit:
  junit: junit
